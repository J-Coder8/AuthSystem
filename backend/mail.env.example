# Copy this file to backend\mail.env and replace the values below.
# Then start the app with run.bat so the backend inherits the credentials.

AUTH_GMAIL_USER=yourgmail@gmail.com
AUTH_GMAIL_APP_PASSWORD=your-16-character-app-password
